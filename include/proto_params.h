c-------------------------------------------------------------------------
c User-definable parameters for the proto program.
c All parameters that are changeable by a user are defined here until a
c more sophisticated way of defining these values is introduced.
c-------------------------------------------------------------------------

      integer buffer_size    
      parameter (buffer_size = 10000)      ! max. size of buffer.
      
